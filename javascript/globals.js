export let getId = function (x) { return document.getElementById(x); };
export let sleep = (ms) => new Promise((r) => setTimeout(r, ms));
;
export const IMSIZE = 1.1;
export let noteTimes = [];
export let doNothing = () => { };
